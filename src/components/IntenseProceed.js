import React from 'react';

export default function IntenseProceed({steps, step, previousStep, triggerNextStep}) {
// Program waits until the user clicks one of the buttons (previously rendered)
    return (
        <div className='className="intenseProceed"'> 
            Click 'Resume' or 'After Care' below  

        </div>
    );
  
}